import { CatalogueView } from "@/components/vitrine/catalogue/CatalogueView";

export const metadata = { title: "Catalogue — Riviere" };

export default function CataloguePage() {
  return <CatalogueView />;
}
